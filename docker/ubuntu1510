FROM ubuntu:15.10

RUN apt-get install -y sudo
RUN useradd -m sho && echo "user:password" | chpasswd && adduser user sudo
RUN echo 'user ALL=(ALL) NOPASSWD: ALL' >> /etc/sudoers
USER user
ADD ./ /home/user
WORKDIR /home/user
RUN ./bootstrap.sh
